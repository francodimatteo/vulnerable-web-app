name: Deploy Vulnerable Application                                  # workflow name

on:
  push:
    branches:
      - master
jobs:
  deploy-heroku:
    name: Deploy Heroku
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: akhileshns/heroku-deploy@v3.12.12 # This is the action
        with:
          heroku_api_key: "HRKU-e8f370fe-e8f7-47b4-9ffd-ccd0f032c042"
          heroku_app_name: "appvulnerablefrancodimatteo" #Must be unique in Heroku
          heroku_email: "francodimatteo6@hotmail.com"
          remote_branch: "master"
          usedocker: true
